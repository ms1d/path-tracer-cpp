# PATH-TRACER

## About

The meat and potatoes of the project!
Polls the requests folder for incoming JSON files (requests) and
carries out the first one (FIFO). The path tracer also streams
outputs in realtime via asio back to the client IP + Port
